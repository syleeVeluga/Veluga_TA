# Whitelabel Screen Checklist

Phase 1 Veluga-owned UI assets are present under `packages/veluga-ui` and renderer adapter components are present under `packages/veluga-renderer`.

Manual screenshot verification is still required for packaged splash, welcome, settings, and about screens after the Electron shim is wired into `packages/cowork-core`.
