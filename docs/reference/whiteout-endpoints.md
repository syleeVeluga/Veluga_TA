# White-Out Endpoint Catalog

Phase 1 code forbids fallback to public LLM endpoints. `packages/veluga-main/src/llm-gateway.ts` requires `VELUGA_LLM_GATEWAY_URL` for the production gateway constructor.

No telemetry or auto-update code is present in this PRD-only workspace.
