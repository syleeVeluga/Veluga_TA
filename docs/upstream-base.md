# Upstream Base

Open Cowork source is not present in this workspace, so no upstream commit SHA can be recorded yet.

Veluga-owned Phase 1/2 code has been implemented as standalone packages under `packages/` and can be integrated into an Open Cowork fork when the source checkout is available.
